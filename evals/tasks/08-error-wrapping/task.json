{
  "prompt": "LoadConfig in config.go formats underlying errors with %v, which destroys the error chain: callers cannot use errors.Is(err, os.ErrNotExist) for a missing file or errors.Is(err, ErrEmpty) for an empty one. Fix LoadConfig so both checks work while keeping the \"load config:\" prefix in the message."
}
